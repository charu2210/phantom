# Phantom Plan — Environment Variables
# Copy this to .env and fill in your values

ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: override model routing
# AGENT1_MODEL=claude-opus-4-5       # Fingerprinting (default: opus — deep analysis)
# AGENT2_MODEL=claude-haiku-4-5      # Classifier (default: haiku — fast)
# AGENT3_MODEL=claude-sonnet-4-5     # Plan builder (default: sonnet)
# AGENT4_MODEL=claude-opus-4-5       # Simulation (default: opus — THE core)
# AGENT5_MODEL=claude-sonnet-4-5     # Guardrails (default: sonnet)
# AGENT6_MODEL=claude-haiku-4-5      # Drift monitor (default: haiku — cost-efficient)
